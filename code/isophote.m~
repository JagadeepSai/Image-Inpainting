function [] = isophote(x,y, Gx, Gy, window, mask)
 = 0;
Gy = 0;
maxG = -1;
for i = x-window:x+window
    for j = y-window:y+window
        if (mask(i,j) == 255)
            if(gradient(i,j)>maxG)
                
    end
end
end