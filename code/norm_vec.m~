function vec = norm_vec(counter_list,pixel,n)
x = pixel(1); y = pixel(2);
window = zeros(n,n);
dist = floor(n/2);
window(dist+1,dist+1) = 1;
for p = counter_list
    x2 = p(1) - x;
    y2 = p(2) - y;
    if( (abs(x1) <= dist) && (abs(y1) <= dist))
        win
    end
end

vec = a;
end

